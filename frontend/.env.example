# Frontend (S3) – set when building for production.
# Copy to .env.local and run: npm run build
# The frontend uses this to call the backend shorten API.

NEXT_PUBLIC_API_URL=https://short.yourdomain.com
